import React from 'react'

const Checkouting = ({ subTitle, title }) => {
    return (
        <div>
            <h1 text-lg font-bold textcolor="#000000" text-weight="252">{title}</h1>
            <h2>{subTitle}</h2>



        </div>
    )
}

export default Checkouting